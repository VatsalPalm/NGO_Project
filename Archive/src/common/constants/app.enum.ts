export enum userRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum eventGender {
  MALE = 'Male',
  FEMALE = 'Female',
  BOTH = 'Both',
}

export enum meetingType {
  ONLINE = 'Online',
  INPERSON = 'inPerson',
}

export enum eventType {
  PUBLIC = 'Public',
  PRIVATE = 'Private',
}
